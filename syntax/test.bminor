// test comment 1
/* one line c */
/*
 multiline not matching in vimregex :(
*/


function int main() = {
	string z = "hello";
	boolean b = true;
	integer x = 0;
	char c = 'x';

	for(x; x < 10; x++){
		if ( (b && true) || (b && false) ){
			print c, x, '\n';
		}
	}

	if(z == "world"){
		print "hello ", z, '\n';
	} else if(z == "hello"){
		print z, " world", '\n';
	}

	return 0;
}
